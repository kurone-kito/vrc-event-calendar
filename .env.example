# Docker Compose defaults
APP_PORT=3000
DB_BIND_HOST=127.0.0.1
DB_PORT=5432
POSTGRES_DB=vrc_event_calendar
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_SCHEMA=public
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# Optional override. Leave empty to use the DATABASE_URL value derived from
# the POSTGRES_* settings in docker-compose.yml.
DATABASE_URL=
